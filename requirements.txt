# Core runtime
arc-agi>=0.9.9,<1.0   # latest published is 0.9.9; pulls in arcengine
numpy>=1.24.0
pillow>=10.0.0
scipy>=1.10.0         # scipy.ndimage.label used by spatial_inference.SpatialReasoner.segment()

# Optional tiers (install on demand):
#   torch, torchvision  -> V-JEPA world model (world_model.py)
#   ollama              -> Nano-LLM HF tier (nano_llm.py)
