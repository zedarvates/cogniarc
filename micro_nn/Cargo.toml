[package]
name = "domain-classifier"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "domain-classifier"
path = "src/main.rs"

[profile.release]
opt-level = "s"   # Size-optimized
lto = true
strip = true
